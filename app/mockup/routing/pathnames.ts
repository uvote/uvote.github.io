import { routePath } from "_/mockup/routing/routes";

export const pathname = {
  listPolls: () => routePath.listPolls,
  myPolls: () => routePath.myPolls,
  home: () => routePath.home,
  pollExample1: () => routePath.pollExample1,
  connectedAccount: () => routePath.connectedAccount,
};
